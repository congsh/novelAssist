## 流程问题
1. 统计信息页获取统计信息正常，但是首页和小说主页的统计信息不对
2. 针对错误和提示的展示
3. 小说页分类展示白色，背景灰白，看不出字

## 报错
1. AI助手初始化遇到报错,实际上可用
```
Access to fetch at 'https://qianfan.baidubce.com/v2/models' from origin 'http://localhost:3000' has been blocked by CORS policy: Response to preflight request doesn't pass access control check: No 'Access-Control-Allow-Origin' header is present on the requested resource. If an opaque response serves your needs, set the request's mode to 'no-cors' to fetch the resource with CORS disabled.
openai-compatible-service.ts:306 
GET https://qianfan.baidubce.com/v2/models net::ERR_FAILED
```
2.
```
Warning: [antd: Tabs] `Tabs.TabPane` is deprecated. Please use `items` instead.
```
3.
```
Warning: Can't call setState on a component that is not yet mounted. This is a no-op, but it might indicate a bug in your application. Instead, assign to `this.state` directly or define a `state = {};` class property with the desired state in the i component.
```